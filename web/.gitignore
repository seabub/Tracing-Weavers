# dependencies
/node_modules

# next.js
/.next/
/out/
/build

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# generated tag/URL sheet
data/tags.csv

# the local passport store holds holder names and emails — never commit it.
# data/passports.example.json is the tracked shape.
data/passports.json
data/passports.json.tmp

# the account store holds password hashes — never commit it either.
data/accounts.json
data/accounts.json.tmp
