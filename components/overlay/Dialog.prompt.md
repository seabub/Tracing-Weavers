Modal with eyebrow, display title, muted body and right-aligned actions.
```jsx
<Dialog open={o} onClose={close} eyebrow="Thank you" title="We'll be in touch within 72 hours." actions={<Button onClick={close}>Done</Button>}>The executive brief is on its way.</Dialog>
```