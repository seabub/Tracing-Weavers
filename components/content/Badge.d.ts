/** Square status chip: CORE, TEMPLATE-BASED, YEAR 1, TO CONFIRM. */
export interface BadgeProps{children:React.ReactNode;tone?:"ink"|"accent"|"amber"|"outline";inverse?:boolean}
export function Badge(props:BadgeProps):JSX.Element;